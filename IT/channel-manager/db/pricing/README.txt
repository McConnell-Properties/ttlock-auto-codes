Nightly-rate CSVs land here.

- `node db/pull-rates.mjs` fetches them from the Google Sheet automatically.
- Or drop manual exports here ("Pull pricing data - <Property>.csv") and run
  `node db/import-rates.mjs`.
